PULL
MainPage: {
	id: autoGenerated
	postSummaryLookupIdList: List<PostSummary>
}

//pull this on post load stage 1 (get the posts)
//List<Section>
Post: {
	postLookupId: uuid
	sectionLookupIdList: List<SectionLookupId> //list of uuid strings, maintains ordering
	postSummaryLookupId: uuid
	authorLookupId: uuid
}

//pull this on main app load
//List<Post>
PostSummary: {
	postSummaryLookupId: uuid
	title: string
	content: {
		image:
		text:
	}
}

//pull this on post load stage 2 (rendering)
//Section
Section: {
	sectionLookupId: uuid
	header: string // V2 may get componatized
	footer: string // V2 may get componatized
    content: {
		type: StringNamedType //x number of properties dependent upon hardcoded named type	
	}
}

SCREENS
HomeScreen -- List of Post Summary views
PostScreen -- post summary at the top, List of Sections each section has the content
AuthoringScreen -- Add Post
PostEditorScreen -- Add Section, Publish
SectionEditorScreen -- header, content, footer
ContentEditorScreen -- image, video, text
PostSummaryEditorScreen -- title image, text fields and complete button



PUSH
flow



//random shit
TwoImagesSideBySide

{
	image1:
	image2:
	text:
}

lookup(type): {
storageObj = 
	getSchema
	_.keys(schema) {
		storageObj.schemaKey = <image>schema.schemaKey</image>
	}
}